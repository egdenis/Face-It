# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'Face It' do
  use_frameworks!
	
  pod 'Firebase'
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'
  target 'Face ItTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
